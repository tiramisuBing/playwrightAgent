# Playwright Test Agent 框架

> 基于 Playwright 官方 Test Agents 的自动化测试生成和维护框架

## 🎯 项目简介

Playwright Test Agent 框架是一个智能化的端到端测试解决方案，利用三个核心 AI 代理（🎭 Planner、🎭 Generator、🎭 Healer）实现测试用例的自动规划、生成和修复。该框架大幅降低测试维护成本，提高测试覆盖率，让测试团队专注于业务逻辑而非重复的代码编写。

### 核心能力

- **🎭 Planner（规划代理）**：探索应用并生成结构化的 Markdown 测试计划
- **🎭 Generator（生成代理）**：将测试计划转换为可执行的 Playwright 测试代码
- **🎭 Healer（修复代理）**：自动检测并修复失败的测试用例

### 适用场景

- Web 应用的端到端测试
- 需要频繁更新的 UI 测试
- 大规模测试套件的维护
- 敏捷开发中的快速测试迭代

---

## 🚀 快速开始

### 前置依赖

- **Node.js**：>= 18.0.0
- **Playwright**：>= 1.48.0
- **AI 工具**（选择其一）：
  - VS Code v1.105+ (Insiders)
  - Claude Code
  - OpenCode

### 安装步骤

1. **克隆或下载项目**

```bash
git clone <repository-url>
cd playwright-test-agent-framework
```

2. **安装依赖**

```bash
npm install
```

3. **安装 Playwright 浏览器**

```bash
npx playwright install
```

4. **初始化代理定义**

根据您使用的 AI 工具选择对应的命令：

```bash
# VS Code
npm run init-agents

# Claude Code
npm run init-agents:claude

# OpenCode
npm run init-agents:opencode
```

这将在 `.github/` 目录下生成代理配置文件。

### 运行示例测试

```bash
# 运行种子测试（验证环境配置）
npx playwright test seed.spec.ts

# 运行所有测试
npm test

# UI 模式运行
npm run test:ui

# 调试模式
npm run test:debug
```

---

## 📚 核心概念

### 测试环境配置

**应用地址**：
- 主应用：https://test.com/
- 登录页：https://tsso.com/login
- 考试中心：https://test.com/

**测试账户**：
- 用户名：xxxxx（手机号）
- 密码：xxxxx

**浏览器支持**：Chromium、Firefox、WebKit

### 三大代理

| 代理 | 职责 | 输入 | 输出 |
|------|------|------|------|
| 🎭 **Planner** | 探索应用，生成测试计划 | 用户需求 + 种子测试 + (可选) PRD | `specs/*.md` |
| 🎭 **Generator** | 将计划转换为代码 | `specs/*.md` + 种子测试 | `tests/*.spec.ts` |
| 🎭 **Healer** | 修复失败的测试 | 失败的测试名称 | 修复后的测试或跳过标记 |

### 项目结构

```
playwright-test-agent-framework/
├── .github/                    # 代理定义（自动生成）
├── specs/                      # 测试计划（Markdown）
│   └── basic-operations.md     # 示例测试计划
├── tests/                      # 测试代码（TypeScript）
│   ├── fixtures.ts             # 自定义 fixtures
│   ├── seed.spec.ts            # 种子测试
│   └── basic-operations.spec.ts # 生成的测试
├── playwright.config.ts        # Playwright 配置
├── package.json
└── README.md
```

### 种子测试（Seed Test）

**作用**：
- 提供测试环境初始化（登录、导航等）
- 执行全局设置和依赖注入
- 作为代码风格模板

**位置**：`tests/seed.spec.ts`

**使用方式**：
- Planner 会运行种子测试来初始化环境
- Generator 会参考种子测试的代码风格
- 所有生成的测试继承种子测试的 fixtures

### 测试计划（Specs）

**格式**：Markdown 文件，存储在 `specs/` 目录

**内容结构**：
- 应用程序概述
- 测试场景分类
- 详细操作步骤
- 预期结果描述

**示例**：查看 [`specs/basic-operations.md`](./specs/basic-operations.md)

---

## 📖 使用指南

### 使用 Planner 生成测试计划

在 AI 工具中使用以下提示词：

```
为 [应用名称] 生成 [功能模块] 的测试计划

**上下文：**
- 种子测试：tests/seed.spec.ts
- 业务场景：[描述用户流程]
- 测试范围：[功能边界说明]

**要求：**
- 覆盖正常流程和异常情况
- 包含边界值测试
- 考虑不同用户权限
```

**注意**：确保将 `tests/seed.spec.ts` 添加到上下文中。

### 使用 Generator 生成测试代码

在 AI 工具中使用以下提示词：

```
根据测试计划 specs/basic-operations.md 生成 Playwright 测试

**上下文：**
- 测试计划：specs/basic-operations.md
- 种子测试：tests/seed.spec.ts

**要求：**
- 使用 Role-based 选择器优先
- 包含清晰的步骤注释
- 遵循种子测试的代码风格
```

**注意**：确保将测试计划文件和种子测试添加到上下文中。

### 使用 Healer 修复失败测试

当测试失败时，在 AI 工具中使用：

```
修复失败的测试：tests/basic-operations.spec.ts

**失败信息：**
[粘贴测试运行错误日志]
```

Healer 会自动：
1. 重播失败的步骤
2. 检查当前 UI 状态
3. 定位等效元素或流程
4. 生成修复补丁
5. 重新运行测试

---

## ⚙️ 配置说明

### Playwright 配置

主要配置项（`playwright.config.ts`）：

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| `testDir` | `'./tests'` | 测试文件目录 |
| `timeout` | `30000` | 单个测试超时时间（毫秒） |
| `retries` | `0` / `2` (CI) | 失败重试次数 |
| `use.baseURL` | `'http://localhost:3000'` | 应用基础 URL |
| `use.trace` | `'on-first-retry'` | 追踪记录策略 |

**修改建议**：
- 根据您的应用更新 `baseURL`
- 调整 `timeout` 以适应应用响应时间
- 配置 `webServer` 以自动启动开发服务器

### 自定义 Fixtures

文件位置：`tests/fixtures.ts`

**内置 Fixtures**：

| Fixture | 类型 | 说明 |
|---------|------|------|
| `authenticatedPage` | Page | 已登录的页面上下文 |
| `testData` | TestDataGenerator | 测试数据生成器（生成登录凭证等） |
| `apiClient` | ApiClient | API 请求客户端 |

**扩展示例**：

```typescript
export const test = base.extend<CustomFixtures>({
  myCustomFixture: async ({}, use) => {
    // 设置逻辑
    const resource = await setupResource();
    await use(resource);
    // 清理逻辑
    await cleanupResource(resource);
  },
});
```

### 更新代理定义

每次 Playwright 版本升级后运行：

```bash
npm run init-agents
```

这将获取最新的工具和指令定义。

---

## 💡 最佳实践

### 种子测试设计

✅ **推荐做法**：
- 保持简单和稳定
- 使用可靠的选择器（Role-based 优先）
- 包含必要的等待策略
- 验证环境就绪状态

❌ **避免做法**：
- 包含复杂的业务逻辑
- 使用脆弱的 CSS 选择器
- 硬编码配置信息

### 测试计划编写

✅ **推荐做法**：
- 原子化步骤（每步一个操作）
- 明确的预期结果描述
- 使用具体的示例数据
- 按逻辑分组场景

❌ **避免做法**：
- 模糊的步骤描述
- 缺少预期结果
- 过于抽象的数据
- 无结构的长列表

### Healer 使用限制

| ✅ 适用场景 | ❌ 不适用场景 |
|-----------|-------------|
| UI 结构微调 | 核心业务逻辑变更 |
| 选择器失效 | 功能完全移除 |
| 时序问题 | 断言逻辑错误 |
| 元素属性变化 | 测试设计缺陷 |

**关键原则**：Healer 的修复必须经过人工审查，避免掩盖真实的应用缺陷。

### 性能优化

1. **并行执行**：利用 Playwright 的 workers 配置
2. **共享上下文**：使用 `test.describe.configure({ mode: 'serial' })`
3. **选择性生成**：针对变更模块生成测试
4. **增量修复**：仅对失败测试运行 Healer

---

## 🔧 故障排查

### 常见问题

**问题 1**：Planner 生成空计划

**原因**：种子测试未成功运行

**解决方案**：
```bash
# 验证种子测试可独立执行
npx playwright test seed.spec.ts --headed

# 查看详细日志
npx playwright test seed.spec.ts --debug
```

---

**问题 2**：Generator 生成的测试立即失败

**原因**：选择器在生成时已失效

**解决方案**：
- 确认应用 UI 稳定
- 在测试环境而非生产环境运行 Generator
- 检查 spec 文件中的步骤描述是否准确

---

**问题 3**：Healer 无限循环

**原因**：修复策略与实际问题不匹配

**解决方案**：
- 检查安全机制配置（重试上限）
- 手动分析失败根因
- 考虑是否为功能缺陷而非测试问题

---

**问题 4**：代理定义未生效

**原因**：AI 工具未识别配置文件

**解决方案**：
- 确认 `init-agents` 使用的 `--loop` 参数正确
- 重启 AI 工具
- 查看 `.github/` 目录是否有生成的文件

---

### 调试策略

**Planner 调试**：
```bash
# 查看种子测试详细输出
npx playwright test seed.spec.ts --reporter=list
```

**Generator 调试**：
```bash
# 以调试模式运行生成的测试
npx playwright test <generated-test>.spec.ts --debug
```

**Healer 调试**：
- 查看 Healer 日志了解尝试的修复策略
- 使用 git diff 对比修复前后的代码
- 启用 trace 查看失败时的页面状态

---

## 📦 示例项目

本框架包含一个 test 应用登录功能的完整测试示例。

### 测试用例概览

| 用例 | 描述 | 涵盖场景 |
|------|------|----------|
| **1.1 使用有效凭证登录** | 验证正常登录流程 | 表单填充、登录按钮点击、页面重定向到考试中心 |
| **1.2 登录失败处理** | 验证错误凭证处理 | 错误输入、错误提示、用户停留在登录页 |

### 文件说明

- **规划文档**：[`specs/basic-operations.md`](./specs/basic-operations.md)
  - 登录场景的详细测试计划、前置条件、步骤、预期结果
  
- **测试代码**：[`tests/basic-operations.spec.ts`](./tests/basic-operations.spec.ts)
  - 两个完整的测试用例（正常登录和失败处理）
  - 使用 Role-based 选择器（`getByRole`、`getByLabel`）增强稳定性
  - 官克登录页面导航、表单交互、URL 等待和断言

- **测试工具**：[`tests/fixtures.ts`](./tests/fixtures.ts)
  - `TestDataGenerator`：生成登录测试凭证
  - `ApiClient`：HTTP 请求客户端
  - `authenticatedPage`：预初始化的已登录页面上下文

### 运行示例

```bash
# 运行登录功能测试
npx playwright test tests/basic-operations.spec.ts

# 以有头模式运行（显示浏览器）
npx playwright test tests/basic-operations.spec.ts --headed

# UI 模式查看实时执行过程
npx playwright test tests/basic-operations.spec.ts --ui

# 生成并查看完整测试报告
npm test
npm run report
```

---

## 🛠️ npm 脚本

| 脚本 | 命令 | 说明 |
|------|------|------|
| `test` | `playwright test` | 运行所有测试 |
| `test:ui` | `playwright test --ui` | UI 模式运行 |
| `test:headed` | `playwright test --headed` | 显示浏览器窗口 |
| `test:debug` | `playwright test --debug` | 调试模式 |
| `report` | `playwright show-report` | 查看测试报告 |
| `init-agents` | `playwright init-agents --loop=vscode` | 初始化 VS Code 代理 |
| `init-agents:claude` | `playwright init-agents --loop=claude` | 初始化 Claude 代理 |
| `init-agents:opencode` | `playwright init-agents --loop=opencode` | 初始化 OpenCode 代理 |

---

## 🤝 贡献指南

### 报告问题

如遇到问题，请提供以下信息：
- Playwright 版本
- AI 工具版本
- 错误日志
- 重现步骤

### 改进建议

欢迎提交以下改进：
- 新的 fixtures 示例
- 测试计划模板
- 最佳实践文档
- Bug 修复

---

## 📄 许可证

本项目采用 MIT 许可证。详见 [LICENSE](./LICENSE) 文件。

---

## 📚 参考资源

- [Playwright 官方文档](https://playwright.dev/)
- [Playwright Test Agents 文档](https://playwright.dev/docs/test-agents)
- [Playwright 最佳实践](https://playwright.dev/docs/best-practices)
- [Playwright API 参考](https://playwright.dev/docs/api/class-playwright)

---

## 🌟 快速链接

- [安装](#安装步骤)
- [使用 Planner](#使用-planner-生成测试计划)
- [使用 Generator](#使用-generator-生成测试代码)
- [使用 Healer](#使用-healer-修复失败测试)
- [配置说明](#配置说明)
- [故障排查](#故障排查)

---

## 💬 支持与反馈

如有问题或建议，欢迎：
- 提交 Issue
- 发起 Pull Request
- 参与讨论

---

**Happy Testing! 🎭**
"# playwrightAgent"  
"# playwrightAgent"  
